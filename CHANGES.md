# CHANGES.md

> Cross-surface session handoff log. Append-only. Read last 2-3 entries at session start. Add new entry at session end.

## Entry Template

```
## B-XXX — YYYY-MM-DD HH:MM — [CLI | Desktop]
**Touched:** files changed
**State:** Working | Broken | Mid-refactor
**Open:** open threads, unfinished work
**Next session:** instructions for the next surface
**Pushed:** yes (commit hash) | no (why)
```

---

## B-014 — 2026-05-01 — CLI
**Touched:**
- lib/pickups-catalog.ts (Pickup type gains optional `coin_value`. New entries: coin_pouch (50), treasure_chest (150), rare_gem (200), glowstone (no coin_value, narration-only)).
- lib/claude.ts (StoryChoice gains optional `coin_cost`, `grants_counter`, `consumes_counter`. All optional; absent on Claude-generated trees so existing flow keeps parsing).
- lib/counters.ts (CounterDef gains optional `start_at`. initialCounters seeds from start_at when set, falling back to max).
- lib/adventures/hunt-dragon-egg.ts (8 new scenes: wolf_encounter, waterfall_climb, eagle_nest, bone_field, crystal_chamber, dragon_cubs, shrine, thief_encounter. New ending_blessed scene + endings entry below ending_friend. New flags: blessing, robbed. Coins counter (max 9999, start_at 50, critical_at 0). Existing scenes rewired: cliff_climb -> wolf_encounter, riverbank gains waterfall + fisher market + river-pony mount, volcano_base gains bone_field, ash_road forwards to dragon_cubs and gains lizard mount + thief, cave_shortcut forwards to dragon_cubs and gains crystal_chamber + lullaby market, lava_chamber forwards to dragon_cubs, forest_path gains shrine. Dragon_chamber narration_variants now react to blessing flag and to has_glowstone derived flag).
- lib/adventures/index.ts (Validator now checks coin_cost requires a coins counter to exist, and grants_counter / consumes_counter only reference declared counter ids).
- lib/sound-bus.ts (new — synthesizes a quick two-tone Web Audio coin chime via OscillatorNode; lazy AudioContext, session-scoped mute key, all errors swallowed).
- components/StoryPlayer.tsx (pickup() awards coin_value to coins counter and chings on treasure pickups. tryActivate() gates on coin_cost like requires, applies consumes_counter then grants_counter on fire, chings if coin counter moved. CHOICE_POSITIONS expanded to 6 slots for the new market+mount-laden riverbank. mergedFlags adds inventory-derived has_<id> booleans so narration variants can key on inventory. Choice tooltip surfaces price hint when coin-short. EconomySummary computed at completion time and threaded through CompletionPayload).
- components/CounterBar.tsx (text display for counters with max>50 so the coins counter renders as "Coins: N" instead of 9999 pips).
- components/RealmCard.tsx (renders the optional EconomySummary as three short lines below the ingredients grid: ending tier label, coins earned/remaining, trophies list).
- app/play/PlayClient.tsx (threads EconomySummary from StoryPlayer through to RealmCard. Hydration fallback for missing counter ids now respects start_at).
- scripts/hunt-dragon-egg-prompts.ts (12 new prompts: 8 scenes + 4 pickup icons).
- public/adventures/hunt-dragon-egg/{wolf_encounter,waterfall_climb,eagle_nest,bone_field,crystal_chamber,dragon_cubs,shrine,thief_encounter}.webp (8 new generated via Replicate).
- public/pickups/{coin_pouch,treasure_chest,rare_gem,glowstone}.webp (4 new generated via Replicate).
- MORNING_CHECKLIST_014.md (new), CHANGES.md.

**State:** Built and deployed to https://realm-shapers.vercel.app. Six clean commits on `main` (29b462f, 08f07a6, f144cca, 4859f41, 5e2db91 plus this CHANGES + checklist push). `npx tsc --noEmit` clean, `npm run lint` clean (0 warnings, 0 errors), validator prints `registry OK`, `npm run build` clean. /play first-load JS at 252 kB (+5 kB from B-013, covers sound-bus + economy plumbing + 8 new scene definitions). **Smoke tests pending Vanessa AM verification per `MORNING_CHECKLIST_014.md`** — agent did not click through any browser flow.

How the scopes connect end-to-end:
1. Schema additions in lib/claude.ts (coin_cost, grants_counter, consumes_counter on StoryChoice) plus coin_value on Pickup. All optional; Claude-generated trees ignore them and keep parsing.
2. Coins counter has start_at: 50 so kids begin with a small buffer. CounterBar switches to text rendering when max>50, hiding the impossibly long pip row 9999 would cause.
3. Sound bus is a tiny lazy-initialized Web Audio chime (no audio file). Honors a sessionStorage mute key. All errors swallowed silently.
4. Eight new scenes plus the existing rewires give the kid 4-5 ways to earn coins (treasure pickups, eagle nest, bone field, waterfall) and 4 ways to spend (shrine, fisher market, hermit market, mounts, thief toll). Mounts are coin-gated express paths (200 coins each) that skip the harder challenges; markets are coin-for-resource trades.
5. The thief on ash_road is the only scene that drains coins involuntarily (or via the run-and-drop choice).
6. dragon_cubs is the new convergence scene before the dragon chamber. All three forward paths (lava, cave, ash) feed into it. The kid gets one last sneak/bribe/fight choice before the final cavern.
7. Realm card now shows the kid what they earned and what they kept. Ending tier labels map every adventure ending scene id to a kid-friendly title ("The Friend", "The Blessed", "The Snatcher", "The Hatcher", etc.).

**Open:**
- **ending_blessed reuses ending_appeased.webp art.** No unique image yet. The brief flagged this explicitly ("tells Vanessa to rerun a generator if she wants a unique one"). Add a prompt for `ending_blessed` to scripts/hunt-dragon-egg-prompts.ts and rerun if you want art parity.
- **First-tap chings can be silent on Safari** due to the autoplay policy. Subsequent transactions ching reliably. Acceptable; the first transaction is usually a small forest_path tap, not the memorable shrine moment.
- **6 choices on the riverbank** is a lot on small screens. CHOICE_POSITIONS now has 6 slots but the layout is dense. If kids find it confusing in playtest, consider consolidating river_crossing + waterfall + mount into a "Cross / Climb / Mount" sub-choice scene later.
- **The robbed flag is wired but unused.** No ending uses it. Future batch could add an ending_robbed tier ("The Robbed", showing coins=0 + the run-and-drop flag).
- **No achievement integration for new scenes.** B-013+ added some achievement triggers; the new scenes (shrine, thief, etc.) do not have achievement defs. Out of scope per the brief.

**Next session:** If smoke tests surface anything broken, the rollback path is `git revert 5e2db91 4859f41 f144cca 08f07a6 29b462f` (six commits, top to bottom). Otherwise consider B-015 entry videos brief next, or polish ending_blessed art.

**Pushed:** yes (5e2db91 plus the CHANGES + checklist commit to follow)

---

## B-012 — 2026-04-30 — CLI
**Touched:**
- lib/themes-catalog.ts (SubScene type gains optional `required_pickups: string[]`. 5 castle sub-scenes now declare gates: castle_library → rusty_key, castle_dungeon → torch, castle_tower_stairs → climbing_rope, castle_dragons_lair → dragons_lullaby, castle_ancient_crypt → ancient_tome. Other 5 themes untouched).
- lib/catalog-validator.ts (validateThemeCatalog now imports PICKUPS_BY_ID and rejects any required_pickups id that doesn't exist in the pickups catalog. Throws at module load).
- public/backgrounds/castle/{outer_gate,courtyard,great_hall,throne_room,dungeon,kitchen,library,royal_chambers,tower_stairs,tower_top,royal_garden,secret_passage,dragons_lair,ancient_crypt}.svg (14 SVGs regenerated from abstract grey blobs to concrete castle iconography per the visual brief: portcullis at the gate, fountain in the courtyard, columns + banners in the great hall, throne + stained glass + crown in the throne room, torches + chains in the dungeon, hearth + pots + flour sacks in the kitchen, floor-to-ceiling shelves + reading desk + scroll in the library, four-poster bed + fireplace + tapestry in the chambers, spiral stair + window slits in the tower, parapet + clouds in the tower top, hedges + fountain + butterflies in the garden, hidden door + cobwebs + torch in the secret passage, gold piles + dragon silhouette + scattered scales in the lair, sarcophagi + cobwebs + skeleton + light shaft in the crypt. Each keeps the small "placeholder: castle_X" label bottom-right. Drawbridge stays as the B-013 webp + entry mp4).
- lib/pickup-resolver.ts (new — resolvePickupRender(id) tries assetUrlById/ASSETS_BY_ID first, falls back to PICKUPS_BY_ID.icon_path. Lets gate items render in StoryPlayer pickup glow and InventoryBar even though they have no asset library entries).
- components/StoryPlayer.tsx (in-scene pickup render now goes through resolvePickupRender. New AMBIENT_TRACK_BY_BACKGROUND map (`castle_drawbridge → "drawbridge"`) wires the ambient bus: per-scene effect calls playAmbient when the background_id maps to a track, stopAmbient when leaving + on unmount. Ambient mute toggle button rendered in the top-right header only on scenes with an ambient track. Mute state mirrored from localStorage via useState + subscribeAmbientMute).
- components/InventoryBar.tsx (top-bar pickup render also routes through resolvePickupRender).
- components/HeroAvatar.tsx (subtle 2px vertical bob loop on a 2.4s ease-in-out cycle, wrapped inside the existing entrance fade so both compose. Bubble + audio sit outside the bob wrapper so the speech bubble doesn't drift).
- lib/ambient-bus.ts (new — mirror of oracle-bus for looping background audio. One shared HTMLAudioElement, fade-in/fade-out on track switch (600ms), volume 0.3, loop on. Mute state lives in localStorage at `realm-shapers:ambient-muted`, separate from the Oracle TTS mute. Respects no-autoplay rule via hasUserInteracted from oracle-bus. Exposes playAmbient, stopAmbient, isAmbientMuted, setAmbientMuted, subscribeAmbientMute).
- app/api/ambient/route.ts (new — POST { track_id } returns { url, cached }. Whitelisted track ids (currently only "drawbridge") prevent arbitrary expensive generations. Generates via ElevenLabs sound-generation, caches mp3 to oracle_voice/ambient/{track_id}.mp3, returns 1-hour signed URL. Auto-creates the bucket if missing).
- lib/elevenlabs.ts (generateSoundEffect now accepts options { durationSeconds, promptInfluence }. Default durationSeconds remains 22 to preserve B-009 behavior. Ambient route passes 22 explicitly).
- lib/claude.ts (level 1 now requires EXACTLY 3 outbound choices per non-ending non-choice scene, up from "2 or 3"; parser bumped from `null` to `3`. Level 2 stays at 5. Tooltip hint instruction rewritten per scope 5 of the brief — tone becomes soft consequence, with 3 verbatim example hints (gate / garden / rusty key) so Claude has a calibration anchor. New buildThemeLibraryBlock additions: each gated castle sub-scene listed with **Requires: X**; new GATE PICKUPS block lists each pickup id + label + description + a hard REQUIRED-PICKUP RULE telling Claude to drop X in scene index 1-4 pickups and to put requires:[X] on the choice leading INTO the gated sub-scene. New isValidPickupOrPropId helper accepts either asset library ids OR pickups catalog ids in scene `pickups` arrays and choice `requires` arrays; phantom-requires check still validates ids appear as a pickup somewhere in the tree).
- MORNING_CHECKLIST_012.md (new), CHANGES.md.

**State:** Built and deployed locally. Seven clean commits on `main`. `npm run lint` clean, `npx tsc --noEmit` clean, `npm run build` clean (route /play first-load JS holds at 247 kB, +1 kB from B-013, the delta covers the ambient bus + pickup resolver + AMBIENT_TRACK_BY_BACKGROUND in StoryPlayer). **Smoke tests pending Vanessa AM verification per `MORNING_CHECKLIST_012.md`** — agent did not click through any browser flow.

How the eight scopes connect end-to-end:
1. Pickups catalog (lib/pickups-catalog.ts) was already shipped by B-013 with all 5 brief-spec'd ids (rusty_key, torch, climbing_rope, dragons_lullaby, ancient_tome) plus a 6th bonus item (dragons_egg). Webp icons live at /pickups/{id}.webp. B-012 keeps everything as B-013 left it; brief intent is satisfied. The 6th id is harmless (Claude may use it as a goal item; not enforced by gates).
2. SubScene gains optional required_pickups; 5 castle sub-scenes declare them; catalog-validator extension throws at module load on any unknown pickup id.
3. 14 castle SVGs replaced. Drawbridge stays as the B-013 webp + entry video; the other 14 are concrete iconography. All keep the `placeholder: castle_X` label so it's clear they're still placeholders, just better ones.
4. Drawbridge polish lives in components: HeroAvatar gets a subtle 2px bob loop; pickup glow + scene fade-in already worked; the in-scene Rusty Key pickup glow works automatically once Claude emits scene.pickups including rusty_key (per the new prompt's GATE PICKUPS section). Castle Guard NPC was optional and skippable; skipped this batch.
5. Ambient sound bus (lib/ambient-bus.ts) + /api/ambient route caches mp3s in the existing oracle_voice bucket. StoryPlayer wires playAmbient when scene = castle_drawbridge, stopAmbient when leaving, mute toggle in the header. Volume 0.3, fade in/out 600ms, loop on, no-autoplay-respected.
6. Level 1 = 3 choices. Single source of truth in lib/claude.ts requiredChoiceCount; prompt + parser both updated. Level 2 (Go Deeper) stays at 5.
7. Tooltip hints rewritten toward soft consequence with 3 verbatim example hints in the prompt as anchors.
8. Other 5 themes untouched in the catalog. Ambient bus only fires on castle_drawbridge (the AMBIENT_TRACK_BY_BACKGROUND map is the gate). The mute toggle button only renders when the current scene has an ambient track. required_pickups gates only exist on 5 castle sub-scenes, so the prompt's GATE PICKUPS block is empty for non-castle realms.

**Open:**
- **Drawbridge water ripple + banner sway animations were not implemented.** The brief's scope 2 called for SVG `<animate>` on a wave path and `<animateTransform>` on a banner element, embedded inside drawbridge.svg. But B-013 superseded drawbridge.svg with a webp + entry mp4, which doesn't have addressable elements to animate. The hero idle bob and pickup glow still apply (component-level), and the entry video already provides motion on first scene mount. If a tighter "feels alive after the video ends" moment is needed, B-013+ could add a small SVG overlay layer with the wave + banner ribbons positioned absolutely over the webp. Not blocking the demo.
- **Castle Guard NPC was skipped.** Brief flagged it as optional ("skip if it bloats — primary value is the key"). The 3 outbound interactables + Rusty Key pickup carry the scene; adding a one-line NPC would require new asset library entries for a guard sprite or a generic-NPC pattern, both of which are bigger than the brief's "optional" framing. Logged for B-013+ if Vanessa wants it.
- **Claude may not always emit the gate pickups.** The prompt now annotates each gated sub-scene with **Requires: X**, lists pickup metadata in a GATE PICKUPS block, and gives a hard REQUIRED-PICKUP RULE. But Claude compliance is probabilistic: in a given realm, Claude may visit the gated sub-scene but never place the required pickup, OR place the pickup but not put `requires` on the inbound choice. The B-010 phantom-requires parser check catches the latter (rejects the tree, retries). The former is silent — the kid simply finds an unlocked door because Claude didn't gate it. If the morning smoke test 4 shows fewer than 3 of 5 gates working, consider either: (a) tightening the prompt with a concrete example tree, (b) adding a parser pass that checks every theme sub-scene with `required_pickups` is gated whenever the tree visits it, or (c) accepting probabilistic gating for the demo.
- **Ambient mp3 generation is on-demand on the first /api/ambient call.** First call takes 10-20 seconds (ElevenLabs SFX latency); subsequent calls return the cached signed URL instantly. To pre-warm: hit `/api/ambient` with `{"track_id": "drawbridge"}` once after deploy so the morning smoke test isn't gated on the generation. Or just visit Drawbridge once before the smoke and accept the silent first 15s.
- **Ambient loop seam may have a subtle click on wraparound.** ElevenLabs sound-generation doesn't guarantee seamless loops. 22s duration was chosen to mask the seam in a kid's attention window. If the click is jarring, B-013+ could either re-generate with a different prompt seed, or post-process with a crossfade in ffmpeg before uploading.
- **Mute toggle is sticky in localStorage but only renders on scenes with an ambient track.** That means a kid who mutes ambient on Drawbridge, then walks to scene 2, then walks back to Drawbridge (if the realm has a return path) will see the toggle reappear in the muted state and can unmute. This is intentional. If they walk to a non-Drawbridge scene first and never come back, the mute state persists silently for the next session.
- **6th pickup (dragons_egg) is in the catalog but has no gate.** B-013 added it as a goal item. B-012 keeps it but doesn't add it to any sub-scene's required_pickups. Claude may or may not pick it up on its own. Decision Open: should the brief's "5 entries" intent be honored by removing dragons_egg, OR should we add a 6th gate? Recommend: leave as-is for the hackathon; add a 6th gate later if Vanessa wants more depth.
- **Pickup resolver fallback works for any theme**, not just Castle. If Claude emits a catalog pickup id (e.g. rusty_key) in a Forest realm, it would render as the Rusty Key icon. The prompt only puts catalog pickup ids in the GATE PICKUPS block when the picked theme has `required_pickups`, so non-castle realms shouldn't see this happen. But a Claude hallucination could leak it. The renderer treats this as a normal pickup with no special meaning; not a bug, just a possibility worth noting.
- **No new DB migration in B-012.** All gating is TS + prompt + parser. The tree shape doesn't change; `requires` arrays already worked since B-007.
- **Old worlds still play.** B-011 and earlier worlds have no required_pickups gates because the field didn't exist when generated. They play normally; required_pickups is purely advisory (only used for prompt-time hints + validator). The renderer doesn't care.
- **No new runtime npm deps.**

**Decisions Open:**
- Drop dragons_egg from the catalog or add a 6th gate for it (see above).
- Whether to add a parser pass that enforces every gated sub-scene has its requires-array on the inbound choice when the tree visits it (would tighten gate compliance to 100 percent at the cost of more retries).
- Whether to add the Castle Guard NPC in a future batch.

**Next session:** After Vanessa walks `MORNING_CHECKLIST_012.md` and confirms green, B-012 ships. Highest-leverage follow-ups: (1) extend the Replicate pilot from B-013 to scale watercolor art to the other 14 Castle sub-scenes + 7 other characters; (2) compress the drawbridge_entry.mp4 (B-013 open thread) from 18.97 MB to ~3-5 MB via ffmpeg; (3) decide on dragons_egg + Castle Guard; (4) if hint quality regresses in the AM smoke, add a worked-example tree to the prompt; (5) if any of the 5 gates fail to fire in 2 of 3 realms, add the parser pass mentioned above.
**Pushed:** pending — final push happens after this entry is committed.
**Production:** https://realm-shapers.vercel.app
**Repo:** https://github.com/vanesvr-lab/realm-shapers

---

## B-013 — 2026-04-30 — CLI
**Touched:**
- lib/image-gen.ts (new — Replicate Flux 1.1 Pro generateImage helper + image-to-video generateVideoFromImage helper that accepts a model slug + per-model input shape and detects the actual file extension from the bytes returned. Plus a verifyModelExists helper that GETs Replicate's models endpoint with the auth token before committing to a candidate).
- scripts/generate-pilot-art.ts (new — generates 8 webp stills then 1 entry video. Skip-if-exists makes it safely re-runnable. 11s throttle between calls to stay under Replicate's 6 req/min low-credit rate limit. Wraps each call in retry-on-429 with backoff. Picks the first reachable image-to-video candidate from kwaivgi/kling-v1.6-pro → lightricks/ltx-video → stability-ai/stable-video-diffusion-img2vid-xt).
- public/backgrounds/castle/drawbridge.webp (new — 256.7 KB, 16:9, generated by Flux 1.1 Pro).
- public/backgrounds/castle/drawbridge_entry.mp4 (new — 18.97 MB, 5s, generated by kwaivgi/kling-v1.6-pro starting from drawbridge.webp).
- public/backgrounds/castle/drawbridge.svg.bak (renamed from drawbridge.svg — preserves rollback path).
- public/characters/wizard.webp (new — 91.1 KB, 1:1, Flux 1.1 Pro). Mirrored to public/assets/characters/wizard.webp so the in-game hero render also picks up the new portrait without a wider catalog refactor.
- public/characters/wizard.svg.bak (renamed from wizard.svg).
- public/pickups/{rusty_key, torch, climbing_rope, dragons_lullaby, ancient_tome, dragons_egg}.webp (new — 6 pickup icons, 47-134 KB each, total ~547 KB).
- lib/themes-catalog.ts (SubScene gains optional `entry_video_path?: string`. Castle drawbridge sub-scene's file_path now points at /backgrounds/castle/drawbridge.webp and gains entry_video_path /backgrounds/castle/drawbridge_entry.mp4. Other 14 castle sub-scenes untouched. Other 5 themes untouched).
- lib/characters-catalog.ts (wizard thumbnail_path now /characters/wizard.webp).
- lib/asset-files.generated.ts (wizard mapped to webp; ext type widened from "svg" | "png" to "svg" | "png" | "webp"). lib/asset-library.ts ext() helper now returns the wider type.
- lib/pickups-catalog.ts (new — 6 pilot pickups: rusty_key, torch, climbing_rope, dragons_lullaby, ancient_tome, dragons_egg. icon_path → /pickups/{id}.webp).
- components/StoryPlayer.tsx (entry-video playback: SUB_SCENES_BY_ID lookup for entry_video_path; sessionStorage gate `realm-shapers:entry-video-played:{world_id}:{scene_id}`; AnimatePresence-wrapped <motion.video> with autoPlay/muted/playsInline/preload="auto"/poster=bgUrl on top of the static background; transparent skip button captures empty-area taps; onEnded + onError both call endEntryVideo which sets sessionStorage and triggers the 200ms opacity exit. Gradient overlay gains pointer-events-none so taps reach the skip button when active. No other render path changed; webp fully transparent to <Image>).
- CHANGES.md.

**State:** Built and deployed locally. Four clean commits on `main`. `npx tsc --noEmit` clean, `npm run build` clean (route /play first-load JS holds at 246 kB, no change from B-011 since the new wiring is small and the video is loaded only on demand).

**Pilot run details:**
- Image-to-video model used: **kwaivgi/kling-v1.6-pro** (first reachable candidate; lightricks/ltx-video also reachable, stability-ai/stable-video-diffusion-img2vid-xt was unreachable on this account).
- Replicate run cost (estimated): ~$0.32 for 8 Flux 1.1 Pro images + ~$0.42 for 1 Kling v1.6 Pro 5s video = **~$0.74 total**. Under the brief's $1 ceiling.
- File sizes: drawbridge.webp 256.7 KB; wizard.webp 91.1 KB; pickups 47-134 KB each (total ~547 KB); drawbridge_entry.mp4 **18.97 MB**.
- The brief's "video under ~5 MB" target was not met — Kling v1.6 Pro returned a high-bitrate 1080p mp4. See "Open" for the implication.

**Open:**
- **Awaiting Vanessa's go/no-go after browser inspection.** Open https://realm-shapers.vercel.app, generate a Castle realm starting at Drawbridge with a Wizard character. Verify: drawbridge.webp renders cleanly, the entry video plays once on scene 1, tap-to-skip works, crossfades to the static image, navigating to scene 2 and back does NOT replay the video, refresh DOES replay. Wizard character renders as the new portrait. Quality call: yes ship the rest (B-014 scales to remaining Castle sub-scenes + 7 other characters + remaining pickups), or fall back to SVG regen.
- **drawbridge_entry.mp4 is 18.97 MB.** Brief target was ~5 MB. Kling v1.6 Pro returned a high-bitrate clip. Three options if Vanessa wants to ship: (a) accept it, video preloads in background while scene 1 renders the still poster — first-paint is unaffected; (b) re-encode with ffmpeg (`ffmpeg -i drawbridge_entry.mp4 -vcodec libx264 -crf 28 -preset slow -an drawbridge_entry_compressed.mp4`) to roughly 3-5 MB and swap the catalog path; (c) regenerate with lightricks/ltx-video next time, which tends to produce smaller files. Recommend (b) as a quick follow-up if pilot is approved.
- **Drawbridge entry video model is hard-coded as kwaivgi/kling-v1.6-pro** in the saved catalog path (mp4 extension). If we later regenerate with a different model that returns webm, the catalog path needs updating. Not a B-013 concern since this is a one-off pilot.
- **Replicate API token leaked into stack-trace stdout** during the first rate-limited run (the ApiError pretty-prints request headers including Authorization). The token `r8_...BbSxa` appeared in the earlier transcript. Recommend rotating REPLICATE_API_TOKEN at https://replicate.com/account/api-tokens after the hackathon, alongside the existing ANTHROPIC_API_KEY rotation note (project memory: project_rotate_anthropic_key.md).
- **Wizard portrait has a pale cream background**, not transparent. Per brief, this is acceptable for the pilot; if it reads as a "sticker" overlay during smoke testing, B-014 follow-up will add a Replicate background-removal pass (e.g. 851-labs/background-remover) before the wizard.webp gets its watercolor cousins.
- **Pickup icons exist on disk + in lib/pickups-catalog.ts but Claude doesn't yet emit these ids in scene.pickups**, so the in-scene/inventory render won't actually exercise them automatically. For now, Vanessa validates pickup icons by opening the .webp files directly (smoke test 2 in the brief). B-014 (or whichever brief teaches Claude about the new pickups) will wire them into actual scene generation.
- **Old SVG placeholders preserved as .bak** at public/backgrounds/castle/drawbridge.svg.bak and public/characters/wizard.svg.bak. To revert pilot: rename .bak back to .svg, set themes-catalog drawbridge file_path back to .svg + drop entry_video_path, set characters-catalog wizard thumbnail_path back to .svg, set asset-files.generated wizard back to "png", remove the StoryPlayer entry-video block. Or just `git revert` the four B-013 commits.
- **Other 14 Castle sub-scenes + other 5 themes untouched.** Per brief: pilot is intentionally narrow.
- **The script's `--force` flag re-spends credits**; default behavior skips files already on disk. Useful when the script has to be retried mid-run (which happened once during this pilot — the first run hit a rate limit on call 2 and the rerun resumed cleanly thanks to skip-if-exists).
- **Replicate image-to-video helpers in lib/image-gen.ts are model-aware via per-slug input shape** in scripts/generate-pilot-art.ts. If a future brief wants to swap models, update buildVideoInput to add the new shape there.
- New runtime npm deps: none. (`replicate` was already in package.json.)

**Next session:** Vanessa's go/no-go. If yes ship: write B-014 to scale to the remaining Castle sub-scenes + 7 other characters + any remaining pickups, plus a one-line ffmpeg compression step for the entry mp4. If fall back: revert the four B-013 commits and continue B-012 SVG regen as the baseline.
**Pushed:** pending — final push happens after this entry is committed.
**Production:** https://realm-shapers.vercel.app
**Repo:** https://github.com/vanesvr-lab/realm-shapers

---

## B-011 — 2026-04-27 — CLI
**Touched:**
- supabase/migrations/0009_worlds_theme.sql (new — adds nullable worlds.theme text column + index for B-011 theme-driven generation; old worlds keep theme = null and route through the B-010 matcher fallback path silently).
- lib/themes-catalog.ts (new — 6 themes × 15 sub-scenes = 90 background nodes with directed connects_to graph for geographic adjacency. Each non-entry sub-scene has ≥2 in-edges. Themes: castle, forest, candy_land, city, space, underwater. Exports THEMES, THEMES_BY_ID, SUB_SCENES_BY_ID, ALL_THEME_IDS, getTheme, getSubScene, isValidThemeId, isValidSubSceneId, subScenesForTheme. validateThemeCatalog runs at module load).
- lib/characters-catalog.ts (new — 8 picker characters: knight, wizard, princess, astronaut, merkid, gingerbread_kid, robot, dragon. Each carries theme_fit list + voice from "Fena" | "Ryan". validateCharacterCatalog runs at module load. ≥1 girl-coded + ≥1 boy-coded enforced).
- lib/catalog-validator.ts (new — runtime validators that throw on init if any catalog cross-reference is broken: theme ids unique, sub-scene ids unique within theme + globally, every connects_to references a real sub-scene in the same theme, every theme has ≥1 entry-candidate + ≥1 ending-candidate, character theme_fit references real themes, ≥1 girl + ≥1 boy).
- public/backgrounds/{theme}/{id}.svg (90 new placeholder SVGs, 15 per theme — base color rect + seeded silhouette shapes + small "placeholder: id" label in the bottom-right corner).
- public/characters/{id}.svg (8 new — silhouette in a colored circle).
- public/themes/{id}.svg (6 new — theme picker thumbnails with theme label + accent silhouettes).
- scripts/generate-b011-placeholders.ts (new — re-runnable generator; idempotent, overwrites).
- components/LandingForm.tsx (rewritten — step-by-step disclosure of theme → setting → character → goal → twist → prompt preview. Each step reveals only after the prior is filled. Step 2 grid = 15 sub-scene cards filtered to picked theme; entry-candidate cards get a "great start" emerald badge; below the grid is a free-text fallback that uses the new matchSubScene helper to narrow within the theme. Step 3 grid = 8 catalog characters always visible regardless of theme; theme-fit characters get a "great match" emerald badge + glow. Step 6 prompt preview composes the assembled prompt as readable prose ("Create my realm in the Castle and Dragons world, starting at the drawbridge, starring my wizard. My goal is to find the dragon's lost egg. The twist is the dragon is shy."). Submits the new theme-driven payload shape. Mobile responsive: 2 columns on narrow phones).
- lib/scene-matcher.ts (new exported matchSubScene helper — same Levenshtein + token-overlap machinery, scores sub-scenes against label + description, returns null below threshold so the form can leave the picker selection alone).
- app/api/generate/route.ts (rewritten — accepts BOTH the new theme-driven shape ({ theme, entry_sub_scene_id, character_id, character_name?, goal, twist }) and the legacy shape ({ setting, character, ...}); validates theme + entry sub-scene + character against the catalogs; composes setting + character prose for the existing prompt phrasing; persists worlds.theme on insert; passes the new structured ids through ingredients so /api/continue and /api/finalize regenerate correctly).
- lib/claude.ts (WorldIngredients gains optional theme_id, entry_sub_scene_id, character_id fields; resolveThemeContext helper reads them and resolves the theme/sub-scene/character from the catalogs; generateWorld branches on theme presence — theme-driven worlds skip the matcher and pass themeContext to the prompt builder; buildStoryPrompt when given themeContext injects a library reference block that lists all 15 sub-scenes with connects_to edges and Entry/Ending flags, locks scene 1 to entry_sub_scene_id, and tells Claude scenes 2-4 must follow connects_to from the previous scene; ALLOWED background_id list and "BACKGROUNDS RANKED" line are dropped in theme mode; new buildThemeHardRules and buildThemeLibraryBlock helpers; hero voice is now resolved server-side from the catalog character.voice and overwrites whatever Claude returned for theme-driven worlds; legacy worlds keep Claude's hero_voice pick).
- lib/claude.ts parser (parseScene gains optional themeSubScenesById Map parameter — in theme mode, background_id MUST be a sub-scene in the picked theme; inline_svg is forbidden; otherwise legacy validation; parseStoryResponse resolves themeContext from ingredients and adds three new validation passes for theme worlds: (1) starting scene's background_id must equal the entry sub-scene id; (2) scenes at indices 2, 3, 4 must use a background_id that appears in the previous scene's connects_to array; (3) ending scenes (choices: [] && !is_choice_scene) must use a sub-scene with can_be_ending: true. Secret_ending parses through the same theme-aware path).
- lib/background-resolver.ts (new — shared resolveBackgroundUrl(id) helper. Tries SUB_SCENES_BY_ID[id].file_path first, falls back to assetUrlById, returns null otherwise so inline_svg can take over. Used by every scene renderer).
- components/StoryPlayer.tsx, components/SceneCanvas.tsx, components/RealmCard.tsx, components/RealmCardThumb.tsx, app/w/[slug]/SharedStoryClient.tsx (all five render paths now resolve scene.background_id through resolveBackgroundUrl, so theme-driven worlds and pre-B-011 worlds render identically without branching).
- CHANGES.md, MORNING_CHECKLIST_011.md (new).

**State:** Built and deployed locally. Eight clean commits on `main`. `npm run lint` clean, `npx tsc --noEmit` clean, `npm run build` clean (route /play first-load JS 246 kB, up from 240 kB; the +6 kB is the catalog imports pulling theme metadata into the client bundle for LandingForm). **Smoke tests pending Vanessa AM verification per `MORNING_CHECKLIST_011.md`** — agent did not click through any browser flow.

How the eight scopes connect end-to-end:
1. Migration 0009 adds `worlds.theme text` (nullable). Old worlds keep `theme = null` and route through the B-010 matcher fallback path silently.
2. Two catalog files (themes, characters) plus a runtime validator define the 6 themes × 15 sub-scenes graph (90 nodes total) and the 8 picker characters with theme_fit + voice. Validators throw at module load on any cross-reference break.
3. 90 background SVGs + 8 character SVGs + 6 theme thumbnails are placeholders generated inline by `scripts/generate-b011-placeholders.ts`. Real watercolor art swaps in via a future asset-gen run without touching the catalog.
4. LandingForm rewrite gates step-by-step: theme → setting → character → goal → twist → prompt preview. The prompt preview is the educational beat — kids see how their ingredients become the prompt and build prompting intuition over time.
5. /api/generate accepts both shapes (new theme-driven and legacy free-text) and persists `worlds.theme` on insert. New shape validates against the catalogs server-side.
6. Claude prompt gets a library reference block when theme_id is present: lists all 15 sub-scenes with connects_to edges, locks scene 1 to entry, tells Claude scenes 2-4 must follow adjacency, scenes 5+ free-form within theme, endings must use can_be_ending sub-scenes. Hero voice is locked server-side from the catalog character.voice (ignores Claude's pick).
7. Parser enforces all of the prompt's structural rules. Trees that violate adjacency, entry-lock, or ending sub-scene rules get rejected and /api/generate retries up to existing limits.
8. resolveBackgroundUrl in lib/background-resolver.ts resolves background ids by trying the sub-scene catalog first then falling back to the asset library. All five renderers route through it so theme-driven and legacy worlds render the same way.

**Open:**
- **BLOCKER: Migration 0009 (`worlds.theme`) MUST be applied before B-011 generation works in production.** Apply via the Supabase SQL editor before the morning smoke. Until applied, the /api/generate insert fails with `column "theme" of relation "worlds" does not exist` for every new world (theme-driven AND legacy), since the route always sets the column (null for legacy). Morning checklist points at this.
- **Sub-scene grid shows ALL 15 cards, including non-entry candidates, and ALL 15 are tappable.** The brief had a tension between "15-card sub-scene grid" (decisions section) and "validate entry_sub_scene_id has can_be_entry: true" (section 6). I resolved by showing all 15, marking entry candidates with a green "great start" badge for visual hint, and relaxing the can_be_entry check on the server (it now only validates the sub-scene belongs to the theme). The kid's choice wins. The catalog's can_be_entry flag is now advisory only (used for UI badges and Claude's prompt context); it is not a server-enforced constraint. Surface as Decision Open: should picking a non-entry as scene 1 be allowed long-term, or should the grid restrict to entry candidates only?
- The matchSubScene helper used in step 2 free-text fallback scores against label + description with a min similarity of 0.45 (lower than the legacy MATCH_MIN_SIMILARITY=0.5 because sub-scene labels are shorter and more specific than category keywords, so true positives sit lower). If the kid types something nonsense, no auto-select happens and they must tap a card.
- Asset library characters and catalog characters share IDs intentionally (knight, wizard, princess, astronaut, robot, dragon all exist in BOTH lib/asset-library.ts CHARACTER_IDS and lib/characters-catalog.ts CHARACTERS). gingerbread_kid and merkid are NEW catalog characters; they will fall through `isValidCharacterId(catalogChar.id)` as false. /api/generate passes character.id as character_asset_id; if that id is not in the asset library, claude.ts skips the picker-asset override and Claude picks from the asset library instead. **This means if a kid picks gingerbread_kid or merkid, the rendered hero will not be the picker thumbnail — it will be whatever Claude picks.** This is the same kind of bug Kellen surfaced (picker said purple dragon, render said hero girl). Surface as Decision Open: either rename gingerbread_kid + merkid to existing asset-library ids, or add new asset-library entries for them, or accept the divergence for now.
- Editor scene-1 background swap (in SceneEditor) is unchanged; the AssetPalette currently lists asset-library backgrounds, NOT sub-scene catalog ids. If a kid swaps the starting scene's background in the editor for a theme world, the rendered scene 1 will show a non-theme background (asset library), bypassing the catalog adjacency for that one scene. The renderer handles both via resolveBackgroundUrl, but Claude's adjacency expectations for scenes 2-4 still presume scene 1 = entry sub-scene. Acceptable for hackathon (it just means the edit-then-play flow can produce a slightly visually inconsistent first scene); fix in a future batch.
- Claude's max_tokens budget did NOT change in B-011. The library reference block adds ~1500 chars to the prompt (15 sub-scenes × ~80 chars each + boilerplate), but the response shape is identical so 6144 (level 1) and 9216 (level 2+) still fit cleanly. If realms start hitting truncation, bump to 8192 / 12288.
- Old worlds (pre-B-011, theme = null) still play normally: WorldIngredients.theme_id is undefined → resolveThemeContext returns null → buildStoryPrompt takes the legacy matcher path → parseScene takes the asset-library + inline_svg path → resolveBackgroundUrl tries the sub-scene catalog, misses, falls back to assetUrlById. No DB migration on `worlds.map` was attempted.
- Picker character "purple dragon" (the existing asset library `dragon` id) is fine because it shares the id. Picker's character thumbnail uses /characters/dragon.svg (B-011 placeholder), but the rendered hero in play uses the asset-library `dragon` PNG (unchanged). They are visually different right now until the placeholder is upgraded; not breaking, just noticeable.
- The legacy CharacterPicker (components/CharacterPicker.tsx) is no longer used by LandingForm but is left in the codebase since its `CHARACTER_PICKER_OPTIONS` and `characterIngredientText` exports are still used by tests / future references. Can be deleted in a future batch.
- The /api/finalize and /api/continue routes were not touched but read theme info from ingredients (since /api/generate persists theme_id + entry_sub_scene_id + character_id into the ingredients JSON column on insert). `generateWorld` resolves themeContext from those, so phase 2 of progressive rendering and Go Deeper both produce theme-conformant trees automatically. Verified by reading both routes; no code changes needed.
- scripts/generate-b011-placeholders.ts is committed and re-runnable. To regenerate placeholders (e.g. after extending the catalog): `npx tsx scripts/generate-b011-placeholders.ts`.
- Mobile responsive: theme grid uses `grid-cols-2 sm:grid-cols-3`, sub-scene grid `grid-cols-2 sm:grid-cols-3`, character grid `grid-cols-2 sm:grid-cols-4`. All three reflow to 2 columns on narrow phones.
- New runtime npm deps: none.

**Next session:** After Vanessa walks `MORNING_CHECKLIST_011.md` and confirms green, B-011 is shipped. Highest-leverage follow-ups in priority order: (1) replace the 90 placeholder background SVGs with real watercolor art (extend the asset-gen flow to also produce the catalog SVGs into public/backgrounds/{theme}/); (2) decide and implement the gingerbread_kid + merkid hero asset story (rename to existing ids or add new asset-library entries); (3) decide whether the sub-scene picker grid should restrict to entry candidates only OR keep all 15 and relax can_be_entry permanently; (4) extend the editor's AssetPalette to show theme catalog sub-scenes when editing a theme-driven world so the editor swap stays inside the theme; (5) prep marketing screenshots showing the new prompt-preview reveal.
**Pushed:** pending — final push happens after this entry is committed.
**Production:** https://realm-shapers.vercel.app
**Repo:** https://github.com/vanesvr-lab/realm-shapers

---

## B-010 — 2026-04-26 → 2026-04-27 — CLI (overnight unattended)
**Touched:**
- lib/claude.ts (phantom-requires hardening from soft-warn to throw; WorldIngredients gains optional character_asset_id + character_name; StoryScene gains optional inline_svg; StoryChoice gains optional hint; StoryTree gains optional level + hero_lines + hero_voice; generateWorld accepts level param; new buildStoryPrompt feeds Claude ranked backgrounds and per-level hints; tree shape rule baked into prompt + parser (endings at scene idx >= 4, idx 0/1 must be regular exploration scenes); level 2+ requires exactly 5 choices per scene + >=5 distinct pickups; hero_lines + hero_voice parser; new generateWorldShell helper synthesizes a 2-scene placeholder for progressive mode without calling Claude).
- lib/backgrounds-catalog.ts (new — 20 entries tagging every background with category + keywords).
- lib/scene-matcher.ts (new — Levenshtein + keyword overlap matcher for setting → ranked backgrounds, MATCH_MIN_SIMILARITY=0.5).
- lib/hero-bus.ts (new — pub/sub mirror of oracle-bus on its own event channel).
- lib/elevenlabs.ts (HERO_VOICE_FENA + HERO_VOICE_RYAN constants + heroVoiceIdFor helper).
- lib/asset-library.ts (5 new placeholder backgrounds: city, school, modern_home, sports_field, lab_hospital).
- public/assets/backgrounds/{city,school,modern_home,sports_field,lab_hospital}.svg (new placeholders).
- components/CharacterPicker.tsx (new — 8-option grid + optional name input; exports CHARACTER_PICKER_OPTIONS with explicit gender_coded tags + characterIngredientText helper).
- components/LandingForm.tsx (rewritten — character free-text replaced with CharacterPicker; reads NEXT_PUBLIC_PROGRESSIVE_GEN env var; sends character_asset_id + character_name + optional progressive flag to /api/generate).
- components/Interactable.tsx (rewritten — two-step tap-to-show / tap-again-to-commit pattern; controlled previewing state via interactableId + isPreviewing + onPreview props; tooltip card renders Claude's hint or a "tap again to go" microcopy; locked hint takes over when present).
- components/HeroAvatar.tsx (new — clickable hero with bouncing 💭 badge, speech bubble, audio playback via /api/oracle-voice with per-realm voice_id; cycles through hero_lines random-no-immediate-repeat).
- components/StoryPlayer.tsx (HeroAvatar replaces inline character motion.div; renderedHeroId resolves heroCharacterId override over story.default_character_id with console.warn sanity; inline_svg dangerouslySetInnerHTML render path with sanitizeInlineSvg; renderedProps merges editorScene1PropIds for starting scene only; previewedInteractableId state with click-outside dismissal; MIN_SCENES_BEFORE_ENDING=4 + DEEP_MIN_PICKUPS_FOR_ENDING=2 runtime gates with reason-aware Oracle lines; new top-right "🚪 Leave realm" button + confirm dialog; "↩ Editor" rename of the existing back-to-editor exit).
- components/SceneEditor.tsx (EditorSnapshot gains propIds field; persists to sessionStorage `realm-shapers:editor-props:{world_id}`; rehydrates on mount; passes the full snapshot to onPlay).
- app/play/PlayClient.tsx (uses SceneEditor's exported EditorSnapshot type; story/title/narration/level/generationStatus all moved to state so Go Deeper and progressive swap work; new handleGoDeeper triggers /api/continue and resets flags + visited; finalize-and-poll effect for progressive status; Go Deeper / Exit buttons on the Realm Card; level badge in header; SceneEditor + StoryPlayer keyed on `${level}-${generationStatus}` so they remount on tree swap).
- app/play/page.tsx (selects + passes generation_status and level).
- app/api/generate/route.ts (overrides default_character_id with picker selection via parseStoryResponse; accepts optional progressive flag → uses generateWorldShell + saves with generation_status='phase_1').
- app/api/continue/route.ts (new — auth-checked + owner-checked POST that regenerates the same world_id at level + 1).
- app/api/finalize/route.ts (new — idempotent POST that flips a phase_1 world to complete by running a full generateWorld call and overwriting worlds.map).
- app/api/world-status/route.ts (new — GET poll endpoint returns status + scenes_ready + map when complete).
- supabase/migrations/0007_worlds_level.sql (new — adds worlds.level integer default 1 + index on (user_id, level)).
- supabase/migrations/0008_world_generation_status.sql (new — adds worlds.generation_status text default 'complete' + index).
- CHANGES.md, MORNING_CHECKLIST_010.md (new).

**State:** Built and deployed. Ten clean commits on `main`. `npm run lint` clean, `npx tsc --noEmit` clean, `npm run build` clean (route /play first-load JS 245 kB, up from 237 kB). **Smoke tests pending Vanessa AM verification per `MORNING_CHECKLIST_010.md`** — agent did not click through any browser flow.

How the ten scopes connect end-to-end:
1. Phantom-requires now throws at parse time so /api/generate's retry path kicks in for any tree where a `requires` item id has no matching pickup. Fixes Kellen's brass-key + hand-mirror bugs at the source.
2. Input-driven assets: kid picks a hero from an 8-option grid (≥1 girl + ≥1 boy, rest CLI's pick); setting free-text is matched against a tagged catalog (15 existing + 5 new placeholders); top-5 ranked backgrounds feed Claude's prompt; if no library match scores >= 0.5, Claude generates per-scene inline SVG backgrounds. Both axes lock the kid's input to the rendered output.
3. Hero render wiring: parseStoryResponse overrides Claude's default_character_id with the picker's asset_id, and StoryPlayer respects the editor's optional swap. Picker is the source of truth.
4. Realm Card Exit + in-game leave-realm: kid can quit at any time. The corner "🚪 Leave realm" surfaces a confirm dialog before routing home.
5. Min-4 scenes gate: structural rule in prompt + parser (no endings at scene idx < 4; idx 0/1 must be exploration scenes); runtime safety belt in StoryPlayer intercepts navigation that would land the kid on a non-secret ending after fewer than 4 distinct visits, holding them on the previous scene with a "still more to explore" Oracle line.
6. Editor placements carry into play: EditorSnapshot.propIds persists per world; StoryPlayer merges them into scene 1's resolved props (editor adds win because the kid placed them deliberately).
7. Go Deeper: Realm Card button POSTs /api/continue, regenerates the same world_id at level + 1 (10-12 scenes, exactly 5 choices per scene, >=5 distinct pickups, ending gated client-side behind 2 collected pickups), bumps worlds.level. Same world_id preserves the B-009 endings log.
8. Choice tooltips: Claude emits a one-sentence flavor `hint` per choice (TONE not consequence). First tap on an Interactable enters a "previewing" state and surfaces the hint; second tap commits; tap a different choice swaps the preview; tap outside dismisses.
9. Clickable hero: StoryTree gains hero_lines (3-5) + hero_voice (Fena | Ryan). HeroAvatar wraps the rendered hero in a button; tap dispatches via lib/hero-bus.ts; HeroAvatar subscribes and plays via /api/oracle-voice with the per-realm voice_id, showing a speech bubble for ~5s.
10. Progressive rendering: with NEXT_PUBLIC_PROGRESSIVE_GEN=true, /api/generate returns an instant client-synthesized 2-scene placeholder ("the realm is forming") with status='phase_1'. PlayClient kicks off /api/finalize and polls /api/world-status every 1.5s; when status flips to 'complete', the full tree swaps in place. Default OFF.

**Open:**
- **Migrations 0007 (`worlds.level`) and 0008 (`worlds.generation_status`) MUST be applied before Go Deeper or progressive rendering work in production.** Apply both via the Supabase SQL editor. Without 0008, the /api/generate insert fails the moment any client sends progressive=true (and even with the flag off, the route is shaped to reference the column). Without 0007, /api/continue fails the level update. Morning checklist points at this.
- **Progressive rendering (scope 10) ships behind `NEXT_PUBLIC_PROGRESSIVE_GEN=false` default.** Vanessa can flip the flag in Vercel env after morning verification. Known limitation: the phase 1 shell is synthesized client-side (no Claude call), so the kid sees a placeholder "the realm is forming" scene initially, not the actual scene 1. Phase 2 then generates the real tree the same way single-call generation does today, just async. Speed savings come from instant first-paint plus SOMETHING-on-screen during the 14-20s wait. A future iteration could have phase 1 be a real Claude call for just 2 scenes for a more polished initial experience.
- The 5 new placeholder background SVGs (city, school, modern home, sports field, lab) are intentionally rough hand-coded SVGs with a "placeholder: X" label in the corner. They exist so the matcher and tagging system work end-to-end. Real watercolor art swaps in via `npx tsx --env-file=.env.local scripts/generate-assets.ts --only city,school,modern_home,sports_field,lab_hospital` whenever Vanessa wants to upgrade them.
- Choice tooltip applies to ALL choices, even those without a Claude-generated hint (uses generic "Tap again to go here" microcopy). This is intentional per brief but is a real UX shift — kids must double-tap every choice now. If kids find it annoying after one playtest, consider single-tap commit when no hint is present.
- Hero voice picking: Claude is instructed via prompt to pick Fena for girl-coded characters (hero girl, princess, fairy, mermaid, witch) and Ryan for boy-coded (hero boy, knight, wizard, pirate, ninja); for neutral characters Claude picks. Defaults to Ryan if Claude omits hero_voice. The picker's CHARACTER_PICKER_OPTIONS array has a `gender_coded` field for future use (currently informational only).
- Editor placements only affect scene 1 per brief. The brief's note about "make pickup-bearing editor props a real, collectable pickup" is NOT implemented in this batch — editor-placed props are merged as decoration only. If Vanessa wants pickup-bearing placements later, extend StoryPlayer's scene.pickups merge to also include any editor prop id whose asset_def is tagged as pickup-able.
- The "min 4 scenes before ending" runtime belt blocks navigation to ending scenes (kid stays on previous scene with choices intact). It does NOT redirect a kid who somehow lands on an ending early. Combined with the structural prompt rule (endings at idx >= 4) the runtime belt should rarely fire.
- Go Deeper consumes more Claude tokens (max_tokens bumped from 6144 to 9216 for level 2+ trees). Generation can take 20-30s for a level 2 tree. The Realm Card button shows "Going deeper..." during the wait; no spinner mini-game (kid just waits with the card visible).
- Old worlds (pre-B-010) play normally: missing fields default to undefined; flags/endings/level/hero_lines/hero_voice/inline_svg all have soft fallbacks.
- The shell tree from generateWorldShell uses scene1's choice to point at scene2 and scene2's choice to point back at scene1, so the kid CAN'T navigate to a not-yet-loaded scene during phase 1. The "kid races phase 2" hold state mentioned in the brief is therefore not exercised in the current implementation; if a future iteration moves to a real Claude-generated phase 1 with choices pointing forward, that hold state will need to be added.
- The /api/continue and /api/finalize routes both call generateWorld synchronously, so a Claude API outage will surface as a 500 to the client. Kid sees the error inline (Realm Card shows "Could not go deeper"; /api/finalize errors are logged but the poll loop just keeps trying). Acceptable for hackathon.
- `world_completed` event now reports `character_id: renderedHeroId` instead of `story.default_character_id`, so achievement counts are based on what the kid actually played (matters when the editor swap deviates from the picker pick).
- Realm Card now shows TWO routes-to-home buttons ("Make another" and "🚪 Exit"). They go to the same place but signal different intents. Discussion-worthy.
- "Forming..." purple banner is visible in edit mode while phase_1; it reuses the SceneEditor key remount on status change so the editor reads the swapped tree's defaults.
- New runtime npm deps: none.

**Next session:** After Vanessa walks `MORNING_CHECKLIST_010.md` and confirms green, B-010 is shipped. Highest-leverage follow-ups: (1) replace the 5 placeholder background SVGs with real watercolor art via the asset-gen script; (2) if kids find double-tap-to-commit annoying, drop it for choices without hints; (3) if progressive rendering proves stable with the flag on, design a smarter phase 1 (real 2-scene Claude call instead of synthesized shell); (4) consider tightening choice tooltips so the hint fades after ~3 seconds of no second tap, instead of staying until tap-elsewhere; (5) audit the actual flag id distribution after ~10 playthroughs and update lib/flag-titles.ts (carryover from B-009).
**Pushed:** pending — final push happens after this entry is committed.
**Production:** https://realm-shapers.vercel.app
**Repo:** https://github.com/vanesvr-lab/realm-shapers

---

## B-009 — 2026-04-26 → 2026-04-27 — CLI (overnight unattended)
**Touched:** lib/claude.ts (StoryTree gains `flags` + `endings`; StoryScene gains optional `flag_set`, `narration_variants`, `prop_overrides`, `is_choice_scene`, `choice_options`; prompt rewritten to teach Claude consequences; strict parser validation rejects undefined flag refs / missing fallback ending / choice scenes without exactly 2 options / prop_overrides on scene 1; max_tokens bumped to 6144; fallback `defaultStory` extended with flags + endings), lib/flags.ts (new — pure helpers `matchesWhen`, `pickVariant`, `setFlag`), lib/scene-resolver.ts (new — `resolveScene` picks narration variant + prop override; `selectEnding` picks ranked ending by flag combo), lib/flag-titles.ts (new — RealmCard title suffix lookup table; direct id matches + substring rules covering common motifs like dragon/treasure/help/secret/brave/magic/forest/sea/sky/song; falls back to no suffix), components/ChoiceMoment.tsx (new — two-button fork UI styled to match Interactable language; large amber-gradient buttons; "Choose" eyebrow label), components/StoryPlayer.tsx (takes `flags` + `onSetFlag` props; uses `resolveScene` for narration + props each render; renders `ChoiceMoment` when `is_choice_scene`; fires `flag_set` on scene EXIT once-per-scene; finale uses `selectEnding` to redirect to flag-resolved ending scene before completing; new gameplay events `choice_made` and `world_completed_with_ending`), app/play/PlayClient.tsx (owns `flags: Record<string, boolean>` state, persists to sessionStorage under `realm-shapers:flags:{world_id}` so refresh resumes; switching worlds remounts and reads that world's key; new "Play again" button on the Realm Card clears flags for the current world; passes `flagTitleSuffix` to RealmCard), components/RealmCard.tsx (accepts optional `flagTitleSuffix` prop, appends to displayed title as "Title, the Suffix"), lib/achievements-types.ts (new triggers `choice_made` + `world_completed_with_ending`; three new defs: `forked_path` 🔀, `two_endings` 🪞, `all_three_endings` 🌈), lib/achievements.ts (CheckEvent extended; `choice_made` unlocks Forked Path; `world_completed_with_ending` upserts into `world_endings` and computes max distinct endings per world to fire Two Endings / All Three Endings), supabase/migrations/0006_world_endings.sql (new — log table keyed on `(user_id, world_id, ending_scene_id)` with RLS), CHANGES.md, MORNING_CHECKLIST_009.md (new).
**State:** Built and deployed. Five clean commits on `main`. `npx tsc --noEmit` clean, `npm run build` clean (route /play first-load JS still 235 kB), `npm run lint` clean, `vercel --prod --yes` deployed to https://realm-shapers.vercel.app. **Smoke tests pending Vanessa AM verification per `MORNING_CHECKLIST_009.md`** — agent did not click through any browser flow.

How the consequence loop works end-to-end:
1. At generation time Claude defines 3-5 named flags (e.g. `tamed_dragon`, `helped_villager`, `chose_treasure`), 1-2 mid-tree choice scenes (`is_choice_scene: true` + 2 `choice_options`), 1-3 scenes with `flag_set` (typically side quests with pickups), 1-3 scenes with `narration_variants`, up to 3 with `prop_overrides`, and a top-level `endings` ranked list with a fallback `{}` entry as last.
2. Strict parser rejects: undefined flag refs in any of `flag_set` / `sets_flag` / `requires` keys / `narration_variants.when` / `prop_overrides.when` / `endings.requires`; choice scenes without exactly 2 options; prop_overrides on the starting scene; fewer than 2 endings; missing fallback as last.
3. At play time PlayClient owns `flags: Record<string, boolean>`, persisted per world in sessionStorage. StoryPlayer pulls flags + onSetFlag from props, resolves narration + props per scene via `resolveScene`, fires `flag_set` once on each scene's first exit, fires the chosen flag on ChoiceMoment activation. Choice scenes route via `goes_to`; the kid can never see the normal interactable layout for those scenes.
4. At finale: secret reroute (visit-all or all-pickups) wins highest priority and lands on `secret_ending`. Otherwise `selectEnding(tree.endings, flags)` resolves to the winning regular ending scene; if it differs from where the kid was routed, StoryPlayer redirects to it and lets the useEffect re-fire so the right narration shows. Then `world_completed`, `world_completed_with_ending` (only for non-secret), and the existing completion flow run.
5. RealmCard appends a flag-derived suffix to the title for the current playthrough. The suffix lookup is intentionally narrow: direct id matches first (e.g. `tamed_dragon` → "the Dragon Tamer"), then substring rules covering ~10 common motifs. Realms whose flag ids don't match anything just show the title with no suffix, which is fine.
6. The `world_endings` table accumulates per (user, world, ending) over time, so the kid can replay a realm twice via "Play again" or by re-entering it from /profile and unlock Two Endings; a third distinct ending unlocks All Three Endings.

**Open:**
- **Migration 0006 must be applied before Two Endings / All Three Endings work in production.** Apply `supabase/migrations/0006_world_endings.sql` via the Supabase SQL editor. Until applied, the upsert errors silently in `evaluateUnlocks` (logged to console) and the cumulative count stays at 0, so neither achievement fires. The morning checklist BLOCKER points at this.
- The "Forked Path" achievement fires from `gameplay_events` directly without needing the `world_endings` table, so it works as long as migration 0005 (B-008) is applied. If a realm Claude generates has no choice scenes (validator only requires the schema fields, not the count), the kid simply won't trigger Forked Path. The prompt asks for 1-2; in practice every generation should have at least one.
- Old worlds in DB (pre-B-009) have no `flags` or `endings` properties. They load fine: `flags` and `endings` on `StoryTree` are optional at the type level (required only at generation), `resolveScene` gracefully falls through to base narration + default props when the optional arrays are missing, `selectEnding` returns null, and StoryPlayer's ending logic short-circuits to existing behavior. No DB migration on `worlds.map` was attempted.
- Generation latency: bumped `max_tokens` from 5120 to 6144 to fit the larger schema (flags + endings + variants per scene). Expect another 1-3 seconds on top of the 14-20 seconds B-008 baseline. Star-tap mini-game continues to keep the kid busy.
- `flag_titles.ts` is a small hand-curated lookup. Direct id rules (`tamed_dragon`, `helped_villager`, etc) are guesses at what Claude commonly emits; substring rules catch the rest. After ~10 Vanessa playthroughs, audit which flag ids show up frequently and add direct rules so the suffix hits more often. Any flag id with no rule match returns no suffix at all.
- ChoiceMoment shows two buttons in the bottom-middle of the scene area; if a scene's narration_variants happen to also fire on the choice scene's flag combo, the variant text shows above the buttons (the resolver runs uniformly). This is intentional: the kid sees the consequence-aware narration as the choice context.
- Choice scenes have `choices: []` (parser enforces). They are NOT counted as endings even though `choices.length === 0`, because StoryPlayer checks `!isChoiceScene && scene.choices.length === 0` for the ending detection. Endings count from the parser side excludes is_choice_scene scenes too.
- Replay button clears `realm-shapers:flags:{world_id}` from sessionStorage but does NOT clear the visited-scenes / inventory state inside StoryPlayer. Those reset naturally because StoryPlayer only mounts when `mode === "play"` and `setMode("edit")` runs first in `handleReplay`.
- The `flag_set` exit firing skips re-firing if the kid revisits the same scene, via a `flagsExitedFor` ref. Necessary to keep flags monotonic per playthrough.
- The choice scene's `goes_to` is validated to point at an existing scene id, but is NOT validated to point at a non-choice / non-side-quest scene. So Claude could chain two choice scenes back-to-back if it wanted; would feel weird, but not broken. Add a stricter rule later if it's a problem.
- Endings that are also side quests are allowed by the parser (a side quest scene with empty choices is technically an ending). Claude's prompt does not forbid this. If a realm pulls this off, the side-quest pill will show on the Realm Card's ending narration; cosmetically fine.
- The "two endings of same world" achievement requires the SAME world_id, so if the kid hits the kind ending in world A and the kind ending in world B, that does NOT unlock Two Endings. Per brief.
- New runtime npm deps: none.
- Still using `claude-opus-4-7`. The brief did not request a model swap.

**Next session:** After Vanessa walks `MORNING_CHECKLIST_009.md` and confirms green, B-009 is shipped. Highest-leverage follow-ups: (1) audit Claude's actual flag id distribution across ~10 generations and add direct-match rules to `flag_titles.ts` so more realms get a suffix; (2) tighten `/api/check-achievements` to validate event payload server-side against an enum (currently still trusts caller); (3) add a small visual cue in edit mode showing how many endings the realm has so the kid knows to replay; (4) consider showing a "you've reached 1/3 endings" hint on the Realm Card to encourage replay; (5) the small profile cleanup of stale B-007 achievement rows mentioned in B-008's open list still applies; (6) prep marketing screenshots for the demo.
**Pushed:** pending — final push happens after this entry is committed.
**Production:** https://realm-shapers.vercel.app
**Repo:** https://github.com/vanesvr-lab/realm-shapers

---

## B-008 — 2026-04-26 → 2026-04-27 — CLI (overnight unattended)
**Touched:** lib/claude.ts (StoryScene.is_side_quest, prompt + parser require 8-10 scenes with 2-3 side quests, expanded fallback story), lib/prop-matcher.ts (new — Levenshtein + tag overlap matcher with id-token boost), app/api/summon-prop/route.ts (new — POST text/world_id/scene_id, returns matched prop_id or suggestion), components/SummonButton.tsx (new — modal + Oracle granted/denied lines + summons-used counter), components/InventoryBar.tsx (rewritten — hosts SummonButton + sparkle on freshly-summoned slots), components/Interactable.tsx (new sideQuest tag prop), components/StoryPlayer.tsx (onEvent callback fires scene_visited + side_quest_completed + pickup_collected + world_completed + secret_ending_discovered + summon_used; side-quest visual marker on scene title; SUMMONS_MAX=5; recentlySummonedId for sparkle), components/LandingForm.tsx (loading copy bumped to "8 to 10 scenes with side quests"), components/ShareWorldButton.tsx (now requires worldId for world_shared event), app/play/PlayClient.tsx (single checkAchievements callback handed to StoryPlayer; removed editor_props_placed flow), app/api/generate/route.ts (REMOVED evaluateUnlocks call — generation never fires achievements anymore; returns unlocked: []), app/api/check-achievements/route.ts (unchanged shape; CheckEvent type rewritten upstream), lib/achievements.ts (rewritten — event-driven evaluator that logs to gameplay_events and reads cumulative state from it; new countSecretsDiscovered + countSideQuestsCompleted helpers), lib/achievements-types.ts (new def list with explicit `trigger` field — first_steps, realm_walker, world_wanderer, story_finisher, five_worlds_strong, all_heroes_tried, treasure_hunter, side_quester, secret_keeper, mystery_master, summoner, master_summoner, share_realm), supabase/migrations/0005_gameplay_events.sql (new — gameplay_events table with RLS), app/profile/page.tsx (new "Side Quests Completed" stat card alongside "Mysteries Discovered"; counters now read from gameplay_events log), app/globals.css (summonSparkle keyframe), CHANGES.md, MORNING_CHECKLIST_008.md.
**State:** Built and deployed. Three pieces of B-008 shipped together:
- **Adhoc prop summoning** (Flavor B-lite, library-match only): inventory bar gains a Summon button. Kid types "a key" / "a glowing flower" / "treasure", the matcher (Levenshtein + tag overlap, id-token hits weighted higher) returns a prop_id or a graceful miss with a suggestion. Matched summons add the prop to inventory with a sparkle, Oracle says "Granted!". Misses say "this realm does not yet hold a [thing], but [a similar prop] lies somewhere ahead, perhaps." Cap is 5 successful summons per playthrough, tracked client-side. NO Replicate / fresh image gen — instant.
- **Longer stories with side quests**: Claude now returns 8-10 scenes total, 2-3 of which are side quests (`is_side_quest: true`). Side quest entries on the main path show a small ✨ side badge on the interactable. Side quest scene titles wear a "✨ side quest" pill. Entering a side quest scene with a pickup triggers an Oracle reward line ("you have earned the X. The realm remembers."). Parser rejects responses with the wrong scene count or side quest count.
- **Achievement timing fix**: every trigger moved from world creation to gameplay. Generating a world fires NO achievements anymore. New defs with explicit `trigger` field. Each gameplay event posts to /api/check-achievements which logs to `gameplay_events` and computes cumulative state from it (distinct scenes / pickups / characters / backgrounds / summoned props / completed worlds / secret worlds). Toasts queue and show one at a time. Profile page now reads side-quest and secret counts from the event log.

`npx tsc --noEmit` clean, `npm run build` clean (route /play first-load JS still 235 kB), `vercel --prod --yes` deployed to https://realm-shapers.vercel.app. **Smoke tests pending Vanessa AM verification per `MORNING_CHECKLIST_008.md`** — agent did not click through any browser flow.
**Open:**
- **Migration 0005 must be applied before achievements unlock in production.** Apply `supabase/migrations/0005_gameplay_events.sql` via the Supabase SQL editor. Until applied, `/api/check-achievements` will swallow the insert error (logged to console) and the response will return an empty `unlocked` array — meaning the kid will play but never see toasts. The morning checklist points at this.
- The new achievements layer trusts client-fired events. A motivated kid could POST achievement events directly. Acceptable for hackathon scale; not for public launch. Tighten before public.
- Cumulative achievements (Realm Walker = 10 distinct scenes, Treasure Hunter = 10 distinct pickups, World Wanderer = all 15 backgrounds, All Heroes Tried = 10 distinct characters, Master Summoner = 10 distinct props, Mystery Master = 5 secret endings, Five Worlds Strong = 5 completed worlds) compute by scanning `gameplay_events`. Index on (user_id, kind) keeps this cheap; if the table grows huge, swap to a materialized counter table.
- Distinct-scene tracking keys on `(world_id, scene_id)` so re-visits in the same world don't double count, but visiting the same scene id across two different generated worlds DOES count as 2 (since worlds have different ids). That feels right: each realm is its own.
- The `summon_used` event with `matched: false` is logged but doesn't unlock anything. Logged anyway so we have data if we want a future "tried 10 wild summons" achievement.
- The previously-removed editor_props_placed achievement (heavy_editor) is GONE. The new def list does not include it. If Vanessa wants a "set designer" badge back, fire it from a new `editor_save` event later.
- Old `share_realm` achievement renamed to id `share_realm` still (compatible). Existing user_achievements rows survive.
- Old `secret_ending` achievement renamed to `secret_keeper` (id changed). Anyone who unlocked the old one will see it as missing; their gameplay_events log won't backfill. Acceptable since this is pre-launch.
- Renamed achievement ids drop ALL old defs that used to fire on world creation: first_realm, three_realms, five_realms, ten_realms, five_characters, five_backgrounds, dragon_friend, wizard_friend, underwater_realm, three_secrets, rare_card, epic_card, legendary_card, heavy_editor, all_pickups, visit_all_scenes. The `user_achievements` table will still have rows for these from B-007 testing; they will just no longer be referenced by `ACHIEVEMENT_DEFS_BY_ID` so they will not show on the profile. Stale rows are harmless. To purge: `delete from user_achievements where achievement_id not in (...current ids...);` after migration.
- Rare/Epic/Legendary card achievements are GONE in the new list. Rarity is still computed and displayed on RealmCard, just not gated to a badge. Add back as `rare_card_first` / etc if Vanessa wants them.
- The `is_side_quest` schema field is required by the parser to be present on every scene (defaults to false on read). Old worlds in DB without this field still load: parseScene treats missing as false. Old world StoryTrees have only 5 scenes which now violates the 8-10 count rule, but only at GENERATION time; load-from-db skips parsing entirely so old worlds replay fine.
- Story length grew so first-load play page is unchanged (235 kB) but Claude generation latency increases ~30-40%, expect 14-20 seconds for the loading screen instead of 10-12. Star-tap mini-game from B-006 still keeps kids busy.
- Summon cap is 5 per playthrough, client-state only. Refresh during play = cap resets. Acceptable since inventory also resets on refresh per B-007.
- `ShareWorldButton` now requires a `worldId` prop. The component is currently NOT used anywhere (RealmCardThumb provides its own share link). Live but inert. Can delete in B-009.
- New runtime npm deps: none.
**Next session:** After Vanessa walks `MORNING_CHECKLIST_008.md` and confirms green, B-008 is shipped. Highest-leverage follow-ups in priority order: (1) plan B-009 (consequences and branching) — brief already exists at `docs/cli-briefs/B-009-consequences-and-branching.md`; (2) tighten /api/check-achievements to validate event payload server-side against an enum (currently trusts caller); (3) add a small "stale rows" cleanup on the profile to drop old B-007 achievement IDs that no longer exist; (4) consider persisting summons_used per playthrough so a refresh doesn't reset it.
**Pushed:** pending — final push happens after this entry is committed.
**Production:** https://realm-shapers.vercel.app
**Repo:** https://github.com/vanesvr-lab/realm-shapers

---

## B-007 — 2026-04-26 → 2026-04-27 — CLI (overnight unattended)
**Touched:** lib/claude.ts (StoryTree schema extension: interactable_kind, pickups, requires, secret_ending), lib/elevenlabs.ts (TTS), lib/oracle-bus.ts (new), lib/achievements.ts (new), lib/achievements-types.ts (new), lib/rarity.ts (new), app/api/oracle-voice/route.ts (new), app/api/check-achievements/route.ts (new), app/api/generate/route.ts (returns unlocked achievements), app/layout.tsx (mount OracleAvatar + page metadata), app/page.tsx (greet via OracleSpeaks), app/play/page.tsx (passes ingredients + username + initial unlocks), app/play/PlayClient.tsx (CeremonyReveal + RealmCard modal + AchievementToast + completion flow), app/profile/page.tsx (card grid + achievements panel + Mysteries counter), components/OracleAvatar.tsx (new, mounted globally with mute toggle), components/OracleSpeaks.tsx (new), components/CeremonyReveal.tsx (new), components/AchievementToast.tsx (new), components/Interactable.tsx (new), components/InventoryBar.tsx (new), components/RealmCard.tsx (new, html2canvas PNG export), components/RealmCardThumb.tsx (new, profile grid), components/StoryPlayer.tsx (rewritten for point-and-click), components/SceneEditor.tsx (lift snapshot to PlayClient), components/LandingForm.tsx (?ceremony=1 navigate + stash unlocks), components/ShareWorldButton.tsx (fire share_realm achievement), supabase/migrations/0004_achievements_and_oracle.sql (new — user_achievements table + oracle_voice bucket), scripts/generate-oracle.ts (new), public/oracle.png (new), package.json (+html2canvas), CHANGES.md, MORNING_CHECKLIST_007.md.
**State:** Built and deployed. Five locked-in pieces shipped together: dramatic ceremony reveal, voiced Oracle (illustrated portrait + cached TTS), 18 server-tracked achievements with toasts and profile panel, downloadable Realm trading cards with rarity tiers (Common→Legendary), and a fully point-and-click play mode that retires the choice buttons in favor of clickable scene icons + inventory + locked-requires-key gating + secret endings. `npx tsc --noEmit` clean, `npm run build` clean (route /play first-load JS 234 kB), `vercel --prod --yes` deployed to https://realm-shapers.vercel.app. **Smoke tests pending Vanessa AM verification per `MORNING_CHECKLIST_007.md`** — agent did not click through any browser flow.
**Open:**
- **Migration 0004 must be applied before Oracle voice or achievements work in production.** It creates the `user_achievements` table (with RLS policies) and seeds the `oracle_voice` storage bucket. Apply via Supabase SQL editor. The /api/oracle-voice route also tries to create the bucket on first call as a fallback, but the migration is the canonical path.
- ElevenLabs TTS: defaults to voice id `21m00Tcm4TlvDq8ikWAM` ("Rachel") at `eleven_turbo_v2_5`. If Vanessa wants a different voice, change `DEFAULT_ORACLE_VOICE_ID` in `lib/elevenlabs.ts`. First-play of any line is 1-3s; subsequent plays of the same `(text, voice_id)` come straight from the bucket signed URL.
- Browser autoplay policy: the Oracle's voice may stay silent on the very first ceremony reveal if the kid's submit click on `/` doesn't carry over the gesture into `/play`. The bubble still appears. After any tap on `/play`, all subsequent Oracle lines play normally for the rest of the session. Acceptable degradation per CLAUDE.md "no autoplay" rule, and Vanessa can verify in the AM smoke.
- Mute toggle (`🔊 Oracle` / `🔇 Oracle muted`) sits below the avatar; state persists in localStorage `realm-shapers:oracle-muted`. The first time a kid clicks the avatar itself, Oracle says a meta line ("I am the Oracle…"). That's intentional, gives them an anchor.
- RealmCard rarity is computed client-side per playthrough (not persisted). Implication: the same world re-played with different exploration produces different rarities, and the profile thumbnails can't show a rarity badge yet. Persisting rarity would need a `worlds.rarity` column; deferred.
- Legendary patterns are hardcoded keyword combos in `lib/rarity.ts` (e.g. shadow + library, dragon + volcano). Add more before public launch if you want more "wow" moments per run.
- Inventory and visited-scenes state are React state only (no localStorage / no `worlds.player_state` JSONB column yet). Refresh during play resets the playthrough. The brief allowed this MVP shortcut.
- Ceremony reveal is gated to URL param `?ceremony=1`, set by `LandingForm` post-generate. Hard-refreshing `/play` does NOT replay it. Skip is enabled at 2s; auto-dismiss at 5s.
- Tutorial overlay ("✨ Click the glowing things to explore") shows once per session in play mode (sessionStorage `realm-shapers:saw-tutorial`).
- Achievement check on `world_created` runs server-side immediately after world insert. Unlocked list returns in `/api/generate` response and is stashed in sessionStorage for `PlayClient` to pop after the ceremony.
- Achievement check on `world_completed` fires client-side after the kid hits an ending. The "all_pickups" condition compares to total pickups across the whole tree (not per-scene), per the brief. "visit_all_scenes" requires hitting all 5 main scene ids in one playthrough.
- "Three secrets" achievement won't auto-unlock yet — we don't track per-completion secret discoveries server-side. The simpler "secret_ending" achievement does fire. If we want the cumulative count, add a `world_completed_at` log table later.
- ShareWorldButton fires `world_shared` on copy. The share_realm achievement should unlock first time a kid shares.
- New runtime npm dep: `html2canvas` (only used client-side via dynamic import). No new server deps.
- The OracleAvatar is hidden on `/preview-3d` because the 3D scene already takes the corner. All other pages get the avatar.
- App metadata title bumped from "Create Next App" to "Realm Shapers" in `app/layout.tsx`.
- Old worlds in DB without the new schema fields still load: `default_props` defaults to [], `pickups` defaults to [], every choice gets `interactable_kind = "path"`. Old worlds will play through with generic green-leaf path icons instead of curated kinds.
**Next session:** After Vanessa walks `MORNING_CHECKLIST_007.md` and confirms green, the demo is feature-complete for May 1. Highest-leverage follow-ups: (1) persist rarity per world so profile thumbnails show badges; (2) per-completion secret-discovery log so cumulative counts (3 secrets, 5 secrets) fire correctly; (3) regenerate the Oracle portrait once or twice to pick the best face — `npx tsx --env-file=.env.local scripts/generate-oracle.ts --force`; (4) responsive polish for narrow phones in play mode (Interactable hit targets, inventory bar overflow); (5) consider per-scene editing for scenes 2-5 so kids can curate the whole tree, not just scene 1.
**Pushed:** pending — final push happens after this entry is committed.
**Production:** https://realm-shapers.vercel.app
**Repo:** https://github.com/vanesvr-lab/realm-shapers

---

## B-000 — 2026-04-24 — Bootstrap
**Touched:** CLAUDE.md, CHANGES.md, .gitignore, .claude/settings.json, docs/*
**State:** Working. Repo scaffolded with kickoff package, no app code yet.
**Open:** None.
**Next session:** Begin B-001 (project scaffold). Read docs/cli-briefs/B-001-project-scaffold.md before starting.
**Pushed:** yes

## B-001 — 2026-04-25 00:30 — CLI
**Touched:** package.json, package-lock.json, tsconfig.json, tailwind.config.ts, postcss.config.mjs, next.config.mjs, .eslintrc.json, app/layout.tsx, app/page.tsx, app/globals.css, app/favicon.ico, app/fonts/*, lib/claude.ts, app/api/generate/route.ts, app/test/page.tsx, CHANGES.md
**State:** Working. Next.js 14 + React 18 + Tailwind 3 scaffolded. Pipeline (4 ingredients to JSON world) verified locally and on Vercel production. Model is claude-opus-4-7.
**Open:**
- Vercel deployment protection may be ON by default for personal projects. If the public hackathon demo URL prompts for login, disable protection in the Vercel dashboard (Project Settings, Deployment Protection, "Only Preview Deployments" or off).
- Vanessa's shell exports an empty ANTHROPIC_API_KEY that shadows .env.local. Local dev must be launched with `unset ANTHROPIC_API_KEY && npm run dev`. Tracking root cause as a follow-up (likely in zshrc/zprofile/zshenv). Saved to project memory.
- ANTHROPIC_API_KEY was pasted in chat during this session. Rotate on or after 2026-05-02 (after the May 1 demo). Saved to project memory with rotation steps.
- Git committer email is auto-detected as elaris@Vanessas-MacBook-Air.local (non-deliverable). Worth setting `git config --global user.email` to a real address.
- Brief specified `claude-sonnet-4-20250514`; bumped to `claude-opus-4-7` per current model defaults. If we want to drop cost later, swap to `claude-sonnet-4-6` or `claude-haiku-4-5` in lib/claude.ts.
**Next session:** Plan and execute B-002 (SVG map rendering). The brief does not exist yet, so first write `docs/cli-briefs/B-002-svg-map.md`, then implement. Touch: a new SVG map component, extend `lib/claude.ts` to also return map data, expand the Claude prompt schema. Do NOT add UI for ingredient input yet (that is B-003).
**Pushed:** yes
**Production:** https://realm-shapers.vercel.app
**Repo:** https://github.com/vanesvr-lab/realm-shapers

## B-002a — 2026-04-25 — CLI (overnight unattended)
**Touched:** lib/supabase.ts, lib/supabase-server.ts, lib/username.ts, lib/username-blocklist.ts, middleware.ts, supabase/migrations/0002_auth.sql, app/api/generate/route.ts, app/test/page.tsx, app/auth/callback/route.ts, app/consent/page.tsx, app/setup-username/page.tsx, app/profile/page.tsx, app/w/[slug]/page.tsx, app/login/page.tsx, app/privacy/page.tsx, components/SaveYourWorldsModal.tsx, components/UsernameInput.tsx, components/ShareWorldButton.tsx, scripts/test-username.ts, package.json, package-lock.json, docs/design-doc.md, CHANGES.md, MORNING_CHECKLIST.md
**State:** Built and deployed. Anonymous-first auth, parent-fronted upgrade via Supabase magic link, COPPA disclosures on `/consent`, kid picks username on `/setup-username`, profile lists worlds with share buttons, public `/w/[slug]` page renders shared worlds without usernames. `npm run build` clean, `npx tsc --noEmit` clean, username smoke test 10/10. **Smoke tests pending Vanessa AM verification per `MORNING_CHECKLIST.md`** — agent did not click magic links or step through browser flows.
**Open:**
- Manual smoke through Flows A, B, C, D in the browser (see `MORNING_CHECKLIST.md`).
- Task 1 manual setup (Supabase project, env vars, migration apply, Vercel env vars) was done by Vanessa before bed; agent skipped Task 1 entirely. `.env.local.example` and the README env-var section from Task 1 steps 6-7 were NOT created. Add later if useful for collaborators.
- `lib/supabase.ts` was split into `lib/supabase.ts` (client-safe `browserSupabase`) and `lib/supabase-server.ts` (`serverSupabase` + `serviceRoleSupabase`) because Next.js fails to compile when a client component imports a module that statically imports `next/headers`. All server-side files import from `@/lib/supabase-server`. Plan called for a single file; this is a deviation worth noting.
- `scripts/test-username.mjs` from the plan was renamed to `scripts/test-username.ts`. Reason: Node 24 swallows the `.mjs` import before tsx sees it, and the import of a `.ts` module fails. Renaming gave tsx ownership.
- `support@realm-shapers.example` is a placeholder. Replace with a real inbox before public launch (mentioned in `/consent` and `/privacy`).
- Privacy notice copy is hackathon-grade. Privacy lawyer review is the explicit "before public launch" step.
- Orphan anon-user cleanup cron not implemented. Acceptable for demo scale, follow-up batch.
- "Save your worlds" button currently lives on `/test` (the dev surface). When B-003 builds the real landing page with the 4-ingredient form, the button moves there too.
- `app/page.tsx` is still the Next.js default splash. B-003 replaces it.
- SUPABASE_SERVICE_ROLE_KEY was selected/displayed in the chat transcript at session start. Treat as exposed and rotate via Supabase dashboard (Settings → API → Reset service role secret) when convenient. Then update `.env.local` and re-run `vercel env add SUPABASE_SERVICE_ROLE_KEY production` (and `preview`).
**Next session:** After Vanessa walks `MORNING_CHECKLIST.md` and confirms green, plan and execute B-002b (SVG map render) or B-003 (kid landing page replacing `/test`).
**Pushed:** yes
**Production:** https://realm-shapers.vercel.app
**Repo:** https://github.com/vanesvr-lab/realm-shapers

## B-006 — 2026-04-26 22:00 → 2026-04-27 — CLI (overnight unattended)
**Touched:** lib/ingredient-seeds.ts (new), lib/asset-library.ts (STYLE_SUFFIX + SCENE_STYLE_SUFFIX rewrite), lib/claude.ts (PropAnimation type, generatePropInteraction), components/IdeaButton.tsx (instant seed list + Oracle path moved behind a button), components/StarTapGame.tsx (new), components/LandingForm.tsx (StarTapGame mounted in loading state), components/InteractiveProp.tsx (new), components/StoryPlayer.tsx (props now wrapped in InteractiveProp), app/api/prop-interaction/route.ts (new), app/globals.css (wiggle/glow/open/pulse keyframes), public/assets/{backgrounds,characters,props}/* (all 95 PNGs regenerated), CHANGES.md, MORNING_CHECKLIST_006.md.
**State:** Built and deployed. The four pieces of kid feedback from B-005 are addressed:
- "Give me ideas" now shows 8-10 hardcoded kid-friendly seeds instantly (no spinner). The Claude path is preserved behind a "🔮 Show me more from the Oracle" button, capped at 3 calls per slot per session.
- Loading screen now hosts a falling-stars tap mini-game with score + timer; auto-disappears when generation completes.
- All 95 assets re-generated via Flux Schnell with a Studio Ghibli watercolor / golden hour atmospheric prompt. New PNGs are visibly more painterly (file sizes roughly 2x the prior batch). 95/95 succeeded, 0 failures.
- In play mode, every default prop is now wrapped in `<InteractiveProp>`. Click → POST `/api/prop-interaction` → Claude returns a one-sentence reaction and an animation choice (wiggle / pulse / glow / open). Speech bubble overlay shows the narration for ~4s. Server-side cache (cap 30) and client-side cache key on `(world_id, scene_id, prop_id)`, so re-clicks are instant.

`npx tsc --noEmit` clean, `npm run build` clean, deployed via `vercel --prod --yes` to https://realm-shapers.vercel.app. **Smoke tests pending Vanessa AM verification per `MORNING_CHECKLIST_006.md`** — agent did not click through any browser flow.
**Open:**
- Per-prop Claude calls add ~2-3s on first click. Aggressive caching mitigates this for repeat clicks within a server lifetime, but Vercel cold starts reset the in-memory cache.
- Flux occasionally drifted away from "no people, no characters" in backgrounds (e.g. `castle_courtyard.png` has a small figure). If any new background looks wrong, regenerate it: `npx tsx scripts/generate-assets.ts --only <id> --force`. Spot-check on a handful of backgrounds (forest, beach, underwater, castle_courtyard, space) and a few characters/props (dragon, oracle, treasure_chest) showed strong painterly improvement and was deemed material enough to keep the run.
- Star-tap game is decorative only (no win state, no escalating difficulty). If kids ask for more substance, swap to a memory-match game in B-007.
- The kid-facing Edit→Play flow gates the prop interactions behind whatever scene composition the editor picks. The interaction data is keyed by `prop_id` rather than the editor-placed prop instance, so two of the same prop in one scene share a cache entry.
- New runtime npm deps: none. (Existing `@anthropic-ai/sdk`, Tailwind animations via globals.css, no new packages.)
- Old prop animation classes are pure CSS in `app/globals.css`, not in tailwind.config.ts. Brief suggested config-level keyframes; globals.css was simpler and self-contained.
**Next session:** After Vanessa walks `MORNING_CHECKLIST_006.md` and confirms green, the four kid asks are addressed. Highest-leverage follow-ups in priority order: (1) if assets are still called bland, plan B-007 visual overhaul (different model or hand-drawn assets); (2) add SFX on prop click to make play mode feel more reactive; (3) per-scene editing (currently only scene 1 editable); (4) tighten share-link auth on `/play` and `/w/[slug]`.
**Pushed:** pending — final push happens after this entry is committed.
**Production:** https://realm-shapers.vercel.app
**Repo:** https://github.com/vanesvr-lab/realm-shapers

## B-005 — 2026-04-26 — CLI (overnight unattended)
**Touched:** lib/asset-library.ts, lib/asset-files.generated.ts, lib/claude.ts, lib/world-audio.ts, scripts/generate-assets.ts, scripts/generate-placeholders.ts, scripts/sync-asset-files.ts, public/assets/{backgrounds,characters,props}/*.svg (95 placeholders), public/3d/kit/{character,nature,platforms}/glTF/*.gltf (Quaternius platformer kit, glTF only), app/page.tsx, app/play/page.tsx, app/play/PlayClient.tsx, app/w/[slug]/page.tsx, app/w/[slug]/SharedStoryClient.tsx, app/api/generate/route.ts, app/api/audio/route.ts, app/api/scene/edit/route.ts, app/preview-3d/page.tsx, app/preview-3d/Preview3DClient.tsx, components/LandingForm.tsx, components/AssetPalette.tsx, components/SceneEditor.tsx, components/SceneCanvas.tsx, components/PropOverlay.tsx, components/TextBubble.tsx, components/StoryPlayer.tsx, components/StylePicker.tsx, components/Forest3DScene.tsx, .gitignore, package.json, package-lock.json, CHANGES.md, MORNING_CHECKLIST_005.md. Deleted: components/WorldMap.tsx, components/IngredientForm.tsx (renamed to LandingForm.tsx).
**State:** Built and deployed. Demo is rebuilt around: a curated 95-asset library (15 backgrounds + 30 characters + 50 props), a drag/resize/layer scene editor with text bubbles and Claude re-narration (cap 5 per session), a 5-scene branching choose-your-own-adventure player with per-scene ambient ElevenLabs audio, and a code-split 3D platformer teaser at /preview-3d (Quaternius character with idle/walk/jump animations, 8 platforms, 3 collectible hotspots). `npx tsc --noEmit` clean, `npm run build` clean, deployed to https://realm-shapers.vercel.app. **Smoke tests pending Vanessa AM verification per `MORNING_CHECKLIST_005.md`** — agent did not click through any browser flow.
**Open:**
- **BLOCKER on real images:** Replicate API returned 402 Payment Required. The asset library shipped with **SVG placeholders for all 95 assets**, not real Flux-generated PNGs. The editor and player work end-to-end with placeholders. To produce real PNGs: (1) add credit to Replicate (https://replicate.com/account/billing), (2) `unset ANTHROPIC_API_KEY && npx tsx scripts/generate-assets.ts` (~10-20 min, ~$1-2 in flux-schnell credits), (3) curate / regen any obviously-broken outputs with `--only id1,id2 --force`, (4) `npx tsx scripts/sync-asset-files.ts` to flip the manifest to .png, (5) commit and redeploy. Walked through in `MORNING_CHECKLIST_005.md`.
- StoryTree generation can take 15-25 seconds end-to-end (Claude opus emits ~2-3 KB JSON for the 5-scene tree). Falls back to a generic default tree if Claude fails twice. Consider swapping to claude-sonnet-4-6 in `lib/claude.ts` if too slow.
- `/api/scene/edit` cap (5 rewrites per user per server lifetime) is held in an in-memory Map, so it resets on Vercel cold starts. Acceptable for demo, not robust against abuse.
- `/api/audio` now keys per scene at `${world_id}/${scene_id}.mp3` in the `world_audio` Storage bucket. Old `${world_id}.mp3` objects from B-002b/003/004 are orphaned but harmless.
- 3D scene relies on `unoptimized` <Image> for asset PNGs/SVGs because Vercel image optimization can be slow/quota-heavy in dev. Worth flipping to optimized once real PNGs land.
- The Quaternius character glTF has 19 animations baked in. Currently used: Idle, Walk, Jump. `Wave`, `Punch`, `Run`, `Dance` etc. are still inside the model and could be wired up to story-specific events later.
- `components/WorldMap.tsx` and old `components/IngredientForm.tsx` were deleted; the new editor flow replaces them. Old `worlds.map` rows from prior batches will hit the "made before the new story system" message on `/play` and `/w/[slug]`.
- Quaternius `Blends/`, `FBX/`, `OBJ/`, `2D/` folders excluded via .gitignore (only glTF is shipped to Vercel, ~6 MB total).
- Optional ambient music at `public/3d/kit/ambient.mp3` is wired up: if Vanessa drops a CC0 track in, the 3D teaser shows a Play music toggle. If absent (current state), a small "Ambient music will be added soon" footer appears.
- `react-three/fiber@^8` (not 9) was pinned because v9 requires React 19; we're on React 18. If we ever upgrade React, bump R3F together.
- New runtime npm deps (approval implicit via brief): `three`, `@react-three/fiber`, `@react-three/drei`, `react-rnd`, `replicate`. `replicate` is technically only needed for the asset-gen script; we kept it as a regular dep so Vanessa can run the script without re-installing. Worth moving to devDependencies later.
**Next session:** After Vanessa walks `MORNING_CHECKLIST_005.md` and confirms green, the demo is shippable for the May 1 deadline. Highest-leverage follow-ups in priority order: (1) get Replicate credit and run the real asset gen so the editor stops looking like placeholder boxes; (2) responsive polish for narrow phones (the AssetPalette stacks under the canvas at <lg, but tap-to-place hasn't been verified); (3) consider per-scene editing in v2 (currently only scene 1 is editable, scenes 2-5 use Claude-picked compositions); (4) swap claude-opus to sonnet if generation latency feels slow during demo.
**Pushed:** pending — final push happens after morning checklist is written.
**Production:** https://realm-shapers.vercel.app
**Repo:** https://github.com/vanesvr-lab/realm-shapers

## B-002b + B-003 + B-004 — 2026-04-25 — CLI (overnight unattended)
**Touched:** lib/claude.ts, lib/elevenlabs.ts, lib/world-audio.ts, supabase/migrations/0003_world_extras.sql, app/api/generate/route.ts, app/api/audio/route.ts, app/api/ideas/route.ts, app/page.tsx, app/play/page.tsx, app/play/PlayClient.tsx, app/profile/page.tsx, app/test/page.tsx (deleted), app/globals.css, components/IngredientForm.tsx, components/IdeaButton.tsx, components/WorldMap.tsx, components/AudioPlayer.tsx, CHANGES.md, MORNING_CHECKLIST_002b.md
**State:** Built and deployed. `/` is now the kid-facing landing (hero + 4-ingredient form with per-slot Claude "give me ideas" suggestions). `/api/generate` returns Claude's title + narration + animated SVG map data + ambient audio_prompt and uploads the ElevenLabs MP3 to the `world_audio` Supabase Storage bucket. `/play?world={id}` walks the character emoji along the map locations with a narration overlay per stop and plays the soundscape behind a Play gesture. End screen offers Make another and Save your worlds (existing B-002a flow). `/test` removed; profile back-link points at `/`. `npm run build` clean, `npm run lint` clean, `npx tsc --noEmit` clean. Vercel `--prod` deploy succeeded. **Smoke tests pending Vanessa AM verification per `MORNING_CHECKLIST_002b.md`** — agent did not click Generate or Play in a real browser.
**Open:**
- Manual smoke per `MORNING_CHECKLIST_002b.md` (incognito → form → generate → /play → audio → save).
- Claude + ElevenLabs are sequential, not truly parallel: ElevenLabs needs the audio_prompt that Claude returns. The brief asked for parallel; the practical version parallelises the storage upload with the DB insert. Total Generate latency observed in build (sequential): ~Claude 10s + ElevenLabs 15s ≈ 25s, matching the brief's worst-case estimate. If this feels too long during demo, swap Claude to `claude-sonnet-4-6` in `lib/claude.ts`.
- `/play` uses service-role read for the world (per brief's hard-refresh note). This means anyone who knows or guesses the world UUID can view a kid's playthrough. UUIDs are unguessable so this is acceptable for hackathon scale, but worth tightening (auth-checked read with share-slug fallback) before public launch.
- `/api/audio` is owner-checked via RLS (auth'd kid replays). Storage bucket `world_audio` is private; only signed URLs (1h TTL) are exposed.
- Default fallback world (used if Claude returns invalid JSON twice) lives in `lib/claude.ts` as `defaultWorld`. It is intentionally generic so the kid never gets stuck. If you see the default scene in the demo, check Claude API health.
- IdeaButton hard-caps "More ideas" at 3 calls per slot per session. Client-side only; not robust against direct API abuse, fine for hackathon.
- Audio is a 22-second loop (ElevenLabs Sound Effects max duration). The walkthrough is ~22 seconds for 5 locations and shorter for 3-4, so it loops cleanly.
- Migration 0003 + the `world_audio` bucket + `ELEVENLABS_API_KEY` env var were applied by Vanessa before this run; not re-verified by the agent.
- Shared world page `/w/[slug]` is still text-only (title + narration + ingredients). Extending it to render the animated map for shareable replay is a natural next batch.
**Next session:** After Vanessa walks `MORNING_CHECKLIST_002b.md` and confirms green, the major hackathon arc is shippable. Remaining nice-to-haves: persistent share view of the animated map (extend `/w/[slug]`), responsive polish for narrow phones, and the orphan anon-user cleanup cron.
**Pushed:** yes
**Production:** https://realm-shapers.vercel.app
**Repo:** https://github.com/vanesvr-lab/realm-shapers
